apiVersion: v1
kind: Service
metadata:
  name: elastic
  labels:
    app: elastic
spec:
  type: ClusterIP
  selector:
    app: elastic
  ports:
    - name: http
      port: 9200
      targetPort: 9200
    - name: transport
      port: 9300
      targetPort: 9300

